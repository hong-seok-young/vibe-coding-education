# 한 번에 붙여넣는 프롬프트 (검증본)

2026-09-08, 사내 PC 에서 7라운드 반복 검증. 뉴스 수집 → DART 수집 → 아웃룩 자동 발송까지
실제로 돌아가는 것을 확인한 프롬프트다. **문장을 줄이면 줄인 자리에서 반드시 막힌다** —
각 문장이 왜 있는지는 `강사가-알려줄것.md` 에 증거와 함께 적어뒀다.

---

## 프롬프트 본문 (여기부터 그대로 복사)

```
파이썬으로 뉴스랑 DART 공시를 모아서 메일로 보내는 프로그램을 만들어줘.
나는 코딩을 전혀 몰라. 윈도우 PC 를 쓰고, 아웃룩에 회사 메일로 로그인해둔 상태야.

검은 터미널 창이 아니라, 더블클릭하면 창이 뜨는 프로그램으로 만들어줘.

[창 구성]
위쪽에 입력 칸 네 개 - 키워드(쉼표로 여러 개), 지켜볼 회사 이름(쉼표로 여러 개),
메일 받을 사람, DART 인증키. 입력한 값은 창을 닫았다 다시 열어도 남아있게 저장해줘.
그 아래에 버튼 네 개 - "뉴스 수집하기", "DART 수집하기", "메일 보내기", "전체 실행".
그 아래는 결과 보는 곳인데 하나로 합치지 말고 세 칸으로 나눠줘 - "뉴스 수집 결과",
"DART 수집 결과", 그리고 진행 상황과 에러가 들어갈 "진행 상황" 칸.
수집 버튼은 각자 자기 칸에만 쓰게 해줘. 세 칸 다 스크롤되고 글자는 고칠 수 없게.

[아주 중요한 공통 규칙 - 이걸 안 지키면 실습이 망한다]
1. 에러가 나면 절대 조용히 넘어가지 마. 프로그램이 아는 모든 실패를 "진행 상황" 칸에
   코딩 모르는 사람이 읽을 수 있는 말로 적어줘. 터미널에만 찍히면 나는 볼 수가 없어.
2. 0건일 때는 "왜 0건인지"를 구분해서 알려줘. 그냥 "0건"은 쓸모가 없어.
3. 버튼을 누른 동안 창이 멈추지 않게 뒤에서 작업해줘. 작업 중에는 버튼이 안 눌리다가
   끝나면 다시 눌리게.
4. 수집량과 화면 출력량에 상한을 둬. 상한이 없으면 결과 칸이 수천 줄이 되어 창이 멈춰.
   화면에는 방금 가져온 것만 앞에서 10건까지, 나머지는 "이 밖에 몇 건 더" 로.

[인터넷에서 받아오는 방식 - 강사님이 적어준 것]
우리 회사는 보안장비가 인터넷 통신을 중간에서 열어보는 환경이라, 파이썬으로 접속하면
CERTIFICATE_VERIFY_FAILED 라는 에러가 난다. 이 부분은 아래 그대로 해줘.

「인터넷에서 자료를 받아올 때 requests 를 쓰지 마라. requests 는 certifi 라는 자기
목록만 신뢰해서 회사 인증서를 모르기 때문에 첫 접속부터 실패한다. 파이썬에 원래 들어있는
urllib 이나 그것을 쓰는 feedparser 를 써라. 이쪽은 윈도우 인증서 저장소를 보기 때문에
회사 인증서가 이미 신뢰돼 있어 그냥 된다. 인증서 검증을 끄는 것은 하지 마라.」

[뉴스 수집]
"뉴스 수집하기" 를 누르면 구글 뉴스에서 기사를 가져와줘. 인증키가 필요 없는 방식이야.
입력한 키워드마다 한국 기준으로 검색해서 키워드당 최신 10건까지만.
키워드 칸이 비어있으면 뉴스 칸에 "키워드가 없어 건너뜀" 만 알려주고 끝내.
못 가져올 때는 "회사 네트워크가 막아서 아예 못 받아온 경우" 와 "받아왔는데 그 키워드로
걸리는 기사가 없는 경우" 를 구분해서 알려주고, 다음 키워드로 계속 진행해줘.
가져온 건수와 검색 결과 전체 건수를 같이 보여줘.

[DART 수집 - 함정이 많으니 강사님이 적어준 것을 그대로]
"DART 수집하기" 를 누르면 전자공시시스템에서 최근 일주일 공시를 가져와줘.
인증키가 없으면 DART 칸에 "DART 인증키가 없어 건너뜀" 만 알려주고 넘어가.

「① DART 에는 회사 이름으로 찾아달라고 요청하는 기능이 없다. corp_name 같은 항목을
   같이 보내도 에러가 나지 않고 status 000 정상 이라고 응답이 오는데, 실제로는 그 값을
   무시하고 전체를 준다. 없는 회사 이름을 보내도 정상이라고 온다. 그래서 요청으로
   거르려 하지 말고, 그 기간 공시를 끝까지 받은 다음 프로그램에서 직접 골라내라.
② 한 번에 받을 수 있는 최대 건수는 100건이다. 그보다 크게 요청해도 에러 없이 100건만
   온다. total_page 를 보고 마지막 페이지까지 넘겨가며 다 받아라. 최근 일주일이면
   보통 2,700건 전후 28페이지이고 15초 정도 걸린다. 몇 페이지째인지 진행 상황에 적어라.
   중간에서 끊으면 뒷페이지에 있던 그 회사 공시를 놓치고 없다고 하게 된다.
③ 목록을 받는 곳은 opendart.fss.or.kr 이지만 문서를 여는 주소는 dart.fss.or.kr 이다.
   원문 주소는 dart.fss.or.kr/dsaf001/main.do?rcpNo=접수번호 형식이다. 받은 자료의
   항목 이름은 rcept_no 지만 주소에 쓸 이름은 rcpNo 다. 이걸 틀리면 「거부」 라고만
   뜨는 페이지가 열린다.
④ 응답의 status 값으로 이유를 구분하라. 000 정상, 013 그 기간에 공시가 아예 없음
   (주말/공휴일이면 정상), 010/011 인증키 문제, 020 요청 한도 초과.」

지켜볼 회사를 적었으면 이름이 정확히 같지 않아도 그 글자가 들어가면 걸리게 해줘
("삼성물산" 처럼 되도록 구체적으로 적을 거야). 회사를 안 적었으면 최신 50건만 담고
"전체 몇 건 중 50건만 담았다" 고 알려줘.
담을 때 제목은 "[회사명] 보고서 이름", 링크는 위 형식의 원문 주소, 날짜는 접수일,
메모는 제출인.
0건이면 ① 그 기간에 공시가 아예 없었는지 ② 공시는 있었는데 그 회사 것이 없었는지
③ 인증키 문제인지 구분해서 알려주고, ② 일 때는 살펴본 전체 건수도 같이 보여줘.
회사를 여러 개 적었으면 "GS건설 1건 / 삼성물산 1건" 처럼 회사별 건수를 한 줄로 요약해줘.

[정리]
모은 항목을 합칠 때 제목이 비어있는 건 빼고, 제목이 똑같으면 링크가 달라도 같은 소식으로
봐서 하나만 남기고, 최신 것부터 정렬하되 날짜가 없는 건 맨 뒤로.
공시는 접수 날짜만 알 수 있고 몇 시인지는 몰라. 시각을 모르는 항목은 날짜만 보여줘.
결과 칸 맨 아래에 "뉴스 몇 건 · DART 몇 건" 처럼 지금까지 모은 걸 출처별로 요약해줘.

[메일 보내기 - 강사님이 적어준 것을 그대로]
"메일 보내기" 를 누르면 모은 항목을 보기 좋은 메일로 만들어서 실제로 발송해줘.

「메일은 SMTP(smtplib)로 보내지 마라. 회사 보안장비가 STARTTLS 단계에서 연결을 끊어서
포트는 열려 있는데 응답이 오지 않고 몇십 초 매달린 뒤 죽는다. 게다가 SMTP 는 계정
비밀번호를 프로그램에 적어야 한다. 대신 윈도우에 이미 로그인되어 있는 데스크톱 아웃룩을
COM 으로 조작해라 (pywin32 의 win32com.client, Dispatch("Outlook.Application"),
CreateItem(0) 으로 메일을 만들고 Send()). 비밀번호가 아예 필요 없다.
본문은 반드시 HTMLBody 에 넣어라. Body 에 넣으면 링크가 「제목 <주소>」 형태의 글자로
깨져서 눌러도 열리지 않는다. 메일은 별도 디자인 파일을 못 불러오니 디자인은 본문 안에
직접 넣어라.」

메일 내용은 맨 위에 "오늘의 이슈 리포트", 오늘 날짜(2026년 09월 08일 형식), 총 몇 건,
"뉴스 몇 건 · DART 몇 건" 요약 한 줄. 본문은 출처별로 나눠서 뉴스끼리 공시끼리 묶고
각각 소제목을 달고 그 안에서 최신순. 항목마다 제목(누르면 원문으로 가는 링크)과 시간.
전체 40건까지만 담고 더 있으면 "이 밖에 몇 건이 더 있습니다" 라고 알려줘.
제목은 "[이슈 리포트] 오늘 날짜 · N건" 형식.

모은 항목이 없으면 "보낼 항목이 없습니다. 먼저 수집하세요" 만 알려주고 끝내.
받는 사람이 비어있으면 "받는 사람이 비어 있습니다" 만 알려주고 끝내.
윈도우가 아니거나 아웃룩 조작에 필요한 게 설치 안 됐으면 그 사실과 설치 방법을 알려줘.
자동 발송이 안 되는 경우(예: 「새 아웃룩」이라 이 방식이 안 통하는 경우)에는, 제목과
받는 사람과 본문이 이미 채워진 메일 작성 창을 열어줘서 내가 [보내기]만 누르면 되게 해줘.

[전체 실행]
"전체 실행" 을 누르면 뉴스 수집 → DART 수집 → 메일 보내기를 순서대로 한 번에.
수집된 게 없으면 메일은 보내지 말고 진행 상황 칸에 알려줘.

마지막으로, 창이 열리면 진행 상황 칸 첫 줄에 실행 중인 파이썬 버전을 한 줄 보여줘.
```

## 검증 결과 (2026-09-08, 사내 PC)

| 확인한 것 | 결과 |
|---|---|
| 창이 뜨고 버튼 4개 동작 | OK |
| 뉴스 수집 (키워드 2개) | 20건, 키워드당 10건 상한 지켜짐 |
| 날짜 한국시간 표시 | `09/08 13:07` |
| DART 28페이지 끝까지 | 2,717건 수집, 15초 |
| 회사 필터 (GS건설·삼성물산) | 2건 정확히 걸림, 회사별 건수 요약됨 |
| DART 원문 링크 실제 열림 | `GS건설/기업설명회(IR)개최(안내공시)/2026.09.07` |
| 창 멈춤 | 0.00초 (뒤에서 작업) |
| 아웃룩 자동 발송 | 보낸 편지함 확인, 본문 10,877자 / 링크 22개 |
| 인증키 틀림 | `DART 인증키에 문제가 있습니다 - 010 등록되지 않은 인증키입니다.` |
| 수집 없이 메일 | `보낼 항목이 없습니다. 먼저 수집하세요.` |
| 받는 사람 없음 | `받는 사람이 비어 있습니다.` |
| 키워드 없음 | `키워드가 없어 뉴스는 건너뜁니다.` |

동작을 확인한 프로그램은 [`news-report-bot/main.py`](news-report-bot/main.py) 다
(설정 저장 기능을 더해 다시 검증했다).

이 프롬프트는 단계별로 쪼개서 `tools/steps.py` 의 STEP 0~6 에 들어가 있고, 실습 페이지는
`python tools/build_page.py` 로 다시 만든다.
