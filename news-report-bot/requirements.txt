feedparser>=6.0.10

# 메일 발송 — 윈도우에 로그인된 아웃룩(데스크톱 앱) 원격 조작용
pywin32>=306

# requests 는 쓰지 않는다. 사내 보안장비가 HTTPS 를 중간에서 열어보는 환경에서
# requests 는 certifi 목록만 신뢰해 회사 인증서를 모르고 첫 접속부터 실패한다.
# 인터넷 접속은 파이썬에 기본 포함된 urllib 과 그것을 쓰는 feedparser 로 한다
# (윈도우 인증서 저장소를 보므로 별도 설정이 필요 없다).
#
# 프로그램 창(GUI)은 파이썬에 기본 포함된 tkinter 를 쓴다 — 따로 설치할 필요 없다
