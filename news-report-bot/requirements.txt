requests>=2.31.0
feedparser>=6.0.10

# 회사 네트워크가 HTTPS를 중간에서 검사하는 환경(TLS 인스펙션) 대응.
# 윈도우가 이미 신뢰하는 인증서 목록을 파이썬도 쓰게 해준다. 없어도 돌아가지만
# (main.py가 대체 방법으로 처리한다) 깔아두면 가장 깔끔하게 해결된다.
truststore>=0.9

# 메일 발송 — 로컬 아웃룩(데스크톱 앱) 원격 조작용
pywin32>=306

# 프로그램 창(GUI)은 파이썬에 기본 포함된 tkinter를 쓴다 — 따로 설치할 필요 없다
