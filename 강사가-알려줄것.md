# 강사가 수강생에게 알려줄 것

한 번에 돌아가는 프롬프트를 수강생이 스스로 쓰게 하려면, 아래 것들을 **미리** 알려줘야 한다.
전부 2026-09-08 사내 PC 에서 실제로 부딪혀 측정한 것이다. 프롬프트에서 해당 문장을 빼면
그 자리에서 다시 막힌다.

---

## 0. 교육 전에 수강생이 준비할 것

| 준비물 | 왜 |
|---|---|
| 파이썬 + `feedparser`, `pywin32` | 당일에 하면 시간이 날아간다. `pip` 은 사내망에서 정상 동작함(확인) |
| **DART 인증키** | 발급에 시간이 걸린다. 뉴스는 인증키가 필요 없어서 먼저 성공을 본다 |
| **클래식 아웃룩에 로그인된 상태** | 자동 발송이 이걸 그대로 쓴다. 「새 아웃룩」이면 이 방식이 안 통함 |

---

## 1. 가장 먼저 가르칠 것 — 「조용한 실패」가 이 실습의 최대 적

일반인 프롬프트로 만든 프로그램이 실패하는 방식은 **에러가 뜨는 것이 아니라 아무 일도
안 일어나거나, 틀린 결과를 성공이라고 말하는 것**이다. 실측 예:

- 버튼을 눌렀는데 결과 칸이 그대로 비어 있음. 에러는 터미널로 빠져서 수강생 눈에 안 보임
- 「지켜볼 회사: 삼성」을 넣었는데 대한전선·남양유업이 나오고, 진행 상황에는
  **`DART 완료: 10건 (전체 2709건)`** 이라고 성공 메시지가 찍힘

그래서 프롬프트에 **"에러가 나면 진행 상황 칸에 사람 말로 적어줘"** 와
**"0건일 때 왜 0건인지 구분해서 알려줘"** 를 반드시 넣게 해야 한다.
이 두 문장이 실습 성공률을 가장 크게 올린다.

---

## 2. 회사 인터넷이 파이썬을 막는다 (SSL) — 라이브러리를 지정해야 한다

**증상**: 브라우저로는 뉴스가 잘 보이는데 프로그램만 `CERTIFICATE_VERIFY_FAILED`.

**원인**: 보안장비가 HTTPS 를 중간에서 열어보기 때문에, 파이썬은 진짜 인증서 대신
보안장비가 만든 인증서를 받는다. 크롬은 회사가 윈도우에 심어둔 루트 인증서를 보므로
문제가 없지만, `requests` 는 윈도우 저장소를 안 보고 `certifi` 라는 자기 목록만 본다.

**실측**

| 접속 방법 | 신뢰하는 루트 | 결과 |
|---|---|---|
| `urllib` (파이썬 기본) / `feedparser` | 67개 = 윈도우 저장소 (회사 인증서 포함) | 성공 |
| `requests` | 121개 = certifi 번들 (회사 인증서 없음) | `self-signed certificate in certificate chain` |

**그래서 프롬프트에 라이브러리를 못 박아야 한다.** 같은 프롬프트를 줘도 AI 가
`feedparser` 를 고르면 되고 `requests` 를 고르면 첫 실행부터 죽는다. **동전 던지기다.**

> ⚠️ 이 저장소의 예전 처방(「`ctx.verify_flags` 의 `VERIFY_X509_STRICT` 만 빼라」)은
> 이 PC 에서 **먹히지 않았다**. 에러 문구도 예전 자료에 적힌
> `Missing Authority Key Identifier` 가 아니라 `self-signed certificate in certificate chain`
> 이었다. 증상이 다르면 처방도 다르다. 그래서 2026-09-08 에 실습 자료 전체를 위 표 기준으로
> 다시 썼고, `tools/check_page.py` 가 예전 처방이 되돌아오면 검사에서 잡아낸다.
> 다만 **교육 전날 강사 PC 와 강의장 PC 에서 한 번씩 재확인할 것.** 확인 방법:
> ```
> python -c "import urllib.request;print(len(urllib.request.urlopen('https://news.google.com/rss/search?q=test',timeout=10).read()))"
> python -c "import requests;print(len(requests.get('https://news.google.com/rss/search?q=test',timeout=10).content))"
> ```
> 앞은 되고 뒤가 죽으면 위 표와 같은 상황이다.

`pip install` 은 사내망에서 정상 동작한다(확인). 설치 단계는 안 막힌다.

---

## 3. DART 는 함정이 세 겹 — 정확한 문구를 그대로 받아 적게 해야 한다

### ① 회사 이름으로 검색이 안 되는데, 에러도 안 난다 (가장 위험)

| 요청 | 응답 | 실제 |
|---|---|---|
| `corp_name=삼성전자` | **status 000 정상** | total_count 2708 (전체와 동일), 회사명은 전부 잡탕 |
| `corp_name=존재하지않는회사이름` | **status 000 정상** | total_count 2708 |

**"정상"이라고 오기 때문에 AI 도 수강생도 필터가 걸린 줄 안다.** 반드시 다 받아온 뒤
프로그램에서 직접 골라내야 한다.

### ② 한 번에 100건이 상한인데, 넘겨도 조용히 잘린다

`page_count=500` → `status 000 정상` + **100건만**. `total_page` 를 보고 마지막
페이지까지 넘겨야 한다. 최근 일주일 = **2,717건 / 28페이지 / 약 15초**.
중간에서 끊으면 뒷페이지의 그 회사 공시를 놓치고 "없다"고 한다.

### ③ 원문 주소는 딱 한 가지만 열린다

| 주소 | 열리는 페이지 |
|---|---|
| `dart.fss.or.kr/dsaf001/main.do?rcpNo=…` | `GS건설/기업설명회(IR)개최(안내공시)/2026.09.07` ✅ |
| `dart.fss.or.kr/dsaf001/main.do?rcept_no=…` | **`거부`** ❌ |
| `opendart.fss.or.kr/dsaf001/main.do?rcpNo=…` | `OpenDART시스템` ❌ |

함정이 두 겹이다 — **응답 필드명은 `rcept_no` 인데 URL 파라미터는 `rcpNo`**,
그리고 **목록은 `opendart` 에서 받지만 문서는 `dart` 에서 열린다.**

### 참고: 되는 파라미터와 무시되는 파라미터가 섞여 있다

`corp_name` 은 무시되지만 `sort`/`sort_mth` 는 실제로 동작한다(`sort_mth=asc` 로
확인). 그래서 "파라미터를 넣었는데 정상 응답이 왔다"는 것이 **작동의 증거가 아니다.**
수강생에게 이 점을 짚어주면 다른 API 를 붙일 때도 도움이 된다.

### ④ 회사 이름은 구체적으로 적게 해야 한다

"삼성" 으로 부분일치하면 최근 일주일 **140건**이 걸리는데 그 내용이:

| 회사 | 건수 |
|---|---|
| 삼성자산운용 | **94건** (전부 ETF 투자설명서) |
| 삼성액티브자산운용 | 14건 |
| 삼성증권 | 9건 |
| 삼성물산 | 1건 |
| **삼성전자** | **0건** |

**「삼성」이라고 적으면 삼성전자는 0건이고 자산운용 ETF 서류 94건이 온다.**
"삼성물산", "삼성전자" 처럼 구체적으로 적게 안내할 것. 회사별 건수 요약 한 줄을
프롬프트에 넣어두면 수강생이 이걸 스스로 알아챈다.

---

## 4. 메일은 SMTP 로 보낼 수 없다 — 이유가 흔히 알려진 것과 다르다

**포트는 막혀 있지 않다.** gmail·office365·naver 의 587 이 전부 열린다. 그래서
"포트가 막혀서"라고 설명하면 수강생이 포트를 확인해보고 SMTP 로 간다.

**실제로 끊기는 지점은 STARTTLS 다.** 세 서버 모두 동일:

```
1) TCP 연결      : OK
2) 인사말(220)   : 220 smtp.gmail.com ESMTP …
3) EHLO 응답     : 250-STARTTLS …   (서버는 지원한다고 광고함)
4) STARTTLS 응답 : 없음 — TimeoutError   <= 여기서 끊김
```

즉 파이썬은 **에러도 없이 몇십 초 매달려 있다가 죽는다.** 여기에 더해 SMTP 는
**계정 비밀번호를 프로그램에 적어야** 한다.

**아웃룩 COM 은 비밀번호가 아예 필요 없다** — 이미 로그인된 아웃룩을 쓴다.
확인: 초안 생성 성공, 보내는 계정은 아웃룩에 로그인된 그 계정, 비밀번호 입력 없음.

**본문은 `HTMLBody` 에 넣어야 한다.** `Body` 에 넣으면 링크가 이렇게 깨진다:

```
Body(평문)으로 보면: '굵게 링크 <https://dart.fss.or.kr> '
```

눌러도 열리지 않는다.

---

## 5. 창이 얼어붙는 문제

프롬프트에 "창이 멈추지 않게" 를 안 넣으면 버튼을 누른 동안 창이 응답하지 않는다.
키워드 1개로도 **1.32초**, DART 는 28페이지라 **15초** 동안 먹통이 된다. 수강생은
"프로그램이 죽었다"고 판단하고 창을 강제 종료한다.

넣었을 때는 **0.00초** (측정값). 진행 상황 칸에 페이지 진행이 찍히니 기다릴 수 있다.

---

## 6. 화면 출력 상한이 없으면 창이 멈춘다

상한 없이 돌린 실측: 키워드 1개 108건 → **326줄**. 회사 필터 「삼성」 → **280줄 /
18,043자**. 키워드 3개면 1,000줄이 넘고 창이 버벅인다.
**방금 가져온 것만 앞에서 10건**, 나머지는 "이 밖에 몇 건 더" 로 줄이게 해야 한다.

---

## 7. 「보여줘」 만으로는 창이 안 나온다

"뉴스 수집해서 보여줘" 라고만 하면 AI 는 **검은 터미널에서 `input()` 으로 키워드를
물어보는 스크립트**를 만든다. 더블클릭할 게 없다.
**"검은 터미널 창이 아니라 더블클릭하면 창이 뜨는 프로그램으로"** 를 반드시 넣게 할 것.

같은 이유로 **"링크도 달아줘"** 만으로는 주소 글자만 나온다.
**"제목을 누르면 브라우저에서 원문이 열리게"** 라고 해야 클릭이 된다.

---

## 8. 아직 검증하지 못한 것 (강의 전 확인 필요)

| 항목 | 상태 |
|---|---|
| **새 아웃룩(New Outlook)에서 COM 실패** | 이 PC 는 클래식 16.0 이라 **재현 못 함**. 새 아웃룩 쓰는 수강생이 있으면 대비 필요 |
| **사내 AI 툴이 `.py` 파일의 역슬래시를 날린다** | 기존 자료에 적힌 내용. 이번에 **확인하지 못했다**. 사실이면 프롬프트에 대응 문구가 필요 |
| **주말/공휴일 status 013** | 하루 단위로는 확인(2026-09-06 일요일 → `013 조회된 데이타가 없습니다`). 일주일 범위에서는 미발생 |
| **DART 요청 한도 초과(020)** | 미발생. 28페이지 × 수강생 20명이면 하루 한도에 닿을 수 있으니 확인 권장 |

---

## 요약 — 프롬프트에 반드시 들어가야 하는 7가지

1. **더블클릭하면 뜨는 창**으로 (안 넣으면 터미널 스크립트)
2. **에러를 화면에 사람 말로** + **0건 이유 구분** (조용한 실패 차단 — 가장 중요)
3. **`requests` 금지, `urllib`/`feedparser` 사용** (SSL 벽)
4. **DART 3가지 함정**: 회사명 검색 안 됨(정상 응답으로 위장) / 100건 상한과 끝까지 페이지 넘기기 / `rcpNo` 주소 형식
5. **메일은 아웃룩 COM + `HTMLBody`** (SMTP 는 STARTTLS 에서 끊김, 비밀번호 필요)
6. **창이 멈추지 않게 뒤에서 작업**
7. **수집량·출력량 상한**


---

## 이 문서와 실습 자료의 관계

2026-09-08 에 위 내용을 반영해 실습 자료를 다시 썼다.

| 파일 | 무엇이 바뀌었나 |
|---|---|
| `tools/steps.py` | STEP 0~6 으로 재작성 (7단계). 보너스 RSS 단계는 검증하지 못해 빼둠 |
| `news-report-bot/main.py` | 검증본으로 교체. `requests` 와 별도 SSL 설정을 쓰지 않는다 |
| `news-report-bot/한국뉴스_점검.py` | `urllib` 과 `requests` 를 나란히 시험해 원인을 갈라주도록 다시 씀 |
| `news-report-bot/dart_점검.py` | 틀린 SSL 처방 제거, 페이지 상한 제거, 회사별 집계 추가 |
| `news-report-bot/requirements.txt` | `requests`·`truststore` 제거 (`feedparser`, `pywin32` 만) |
| `파이썬 설치.bat` | 설치 목록에서 `requests` 제거 |
| `tools/check_page.py` | 틀린 처방이 되돌아오면 검사에서 잡히도록 낡은 표현 목록에 추가 |
| `README.md` · `news-report-bot/README.md` | SSL·SMTP 설명을 실측값으로 교체, 진행표 재조정 |

실습 페이지를 다시 만들려면 `python tools/build_page.py` 를 실행한 뒤
`python tools/check_page.py` 로 점검한다.
